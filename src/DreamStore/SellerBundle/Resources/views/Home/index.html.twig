Hello !

{% for product in products %}
    {{ product.name }}<br>
    {{ product.description }}<br>
    {{ product.price }}<br>
    {{ product.stock }}<br>
    <img style="width: 150px; height: 150px;" src="{{ asset("DS/" ~ product.imageName) }}" alt="{{ product.imageName }}">

{% endfor %}s